# Review Summary AI - 프로젝트 종합 가이드

> **대상 독자**: 비개발자 (운영팀, 기획자, 관리자)
> **최종 수정**: 2026-04-01

---

## 목차

1. [시스템 개요](#1-시스템-개요)
2. [기술 스택](#2-기술-스택)
3. [폴더 구조](#3-폴더-구조)
4. [데이터베이스 구조](#4-데이터베이스-구조)
5. [핵심 기능 설명](#5-핵심-기능-설명)
6. [워크플로우 (데이터 흐름)](#6-워크플로우-데이터-흐름)
7. [분석 알고리즘](#7-분석-알고리즘)
8. [API 주소 목록](#8-api-주소-목록)
9. [화면(대시보드) 구성](#9-화면대시보드-구성)
10. [주요 파일별 함수 설명](#10-주요-파일별-함수-설명)

---

## 1. 시스템 개요

### 한줄 요약

**Carmore 렌트카 22만 건의 고객 리뷰를 AI가 자동 분석하여, 운영팀이 지점별 서비스 품질을 한눈에 파악할 수 있는 모니터링 대시보드 시스템**

### 왜 만들었나

- Carmore 플랫폼에 **22만 건 이상의 리뷰**가 쌓여 있지만, 사람이 하나하나 읽고 분석하는 것은 불가능
- 운영팀은 **"어떤 지점이 잘하고 있고, 어떤 지점에 문제가 있는지"** 빠르게 파악해야 함
- 이 시스템이 리뷰를 **자동 수집 → 감정 분석 → 주제 분류 → AI 요약 → PDF 리포트** 까지 처리해줌

### 누가 사용하나

| 사용자 | 하는 일 | 접속 방식 |
|--------|---------|-----------|
| **국내사업팀** | AI 리포트를 생성하여 파트너스(렌트카 업체)에 전달 | 웹 브라우저 (대시보드) |
| **운영팀** | 대시보드에서 지점별 서비스 품질 모니터링, 요약 확인 | 웹 브라우저 (대시보드) |
| **파트너스 (렌트카 업체)** | 전달받은 AI 리포트 확인, 또는 자기 지점 요약/리포트 직접 조회 | API 키 인증 |
| **자동 스케줄러 (n8n)** | 매일 자동으로 리뷰 동기화, 요약 생성 | 웹훅 자동 호출 |

**핵심 업무 흐름:** 국내사업팀이 AI 리포트를 만들어 파트너스에 전달 / 운영팀이 지점 상태를 모니터링

### 핵심 숫자

| 항목 | 규모 |
|------|------|
| 총 리뷰 수 | 222,024건 |
| 렌트카 지점 수 | 1,408개 |
| 렌트카 업체 수 | 390개 |
| 분류 태그 수 | 52개 (9개 카테고리) |
| 차량 모델 수 | 553종 |

---

## 2. 기술 스택

> "이 시스템을 만드는 데 어떤 도구를 썼나?"

### 쉬운 비유

| 역할 | 사용 기술 | 비유 |
|------|-----------|------|
| **서버 (백엔드)** | Python + FastAPI | 식당의 주방 - 주문을 받고 요리를 만드는 곳 |
| **데이터 저장소** | PostgreSQL (Supabase) | 식당의 냉장고 - 모든 재료(데이터)를 보관 |
| **AI 요약 엔진** | OpenAI GPT-4o-mini | 전문 요리사 - 재료를 받아서 요리(요약)를 만듦 |
| **한국어 분석기** | Kiwi | 재료 손질 담당 - 한국어 문장을 단어 단위로 쪼갬 |
| **태그 분류기** | FastEmbed (ONNX) | 재료 분류 담당 - "이 리뷰는 청결에 대한 이야기" 판별 |
| **리뷰 원본 저장소** | AWS Athena | 큰 창고 - 원본 리뷰가 대량 보관된 곳 |
| **화면 (프론트엔드)** | HTML + JavaScript | 식당 홀 - 손님(운영팀)이 보는 공간 |
| **PDF 생성** | WeasyPrint | 인쇄소 - 리포트를 PDF 문서로 만들어줌 |
| **자동화 도구** | n8n | 타이머 - 매일 정해진 시간에 알아서 작업 실행 |

### 상세 기술 목록

| 분류 | 기술 | 용도 |
|------|------|------|
| 언어 | Python 3.12 | 전체 서버 코드 작성 |
| 웹 프레임워크 | FastAPI | API 서버, 웹 요청 처리 |
| 데이터베이스 | PostgreSQL (Supabase) | 리뷰, 요약, 태그 등 모든 데이터 저장 |
| DB 연결 | SQLAlchemy Async + asyncpg | 데이터베이스와 비동기 통신 |
| DB 마이그레이션 | Alembic | 데이터베이스 구조 변경 관리 (61개 버전) |
| AI/LLM | OpenAI GPT-4o-mini | AI 요약 텍스트 생성, 리포트 작성 |
| 한국어 NLP | Kiwi | 한국어 형태소 분석 (문장 → 단어 분리) |
| 임베딩 | FastEmbed (ONNX) | 문장 의미 비교 (태그 자동 분류용) |
| 데이터 처리 | pandas, openpyxl | 엑셀 파일 처리, 데이터 분석 |
| PDF | WeasyPrint | 리포트 PDF 생성 |
| 외부 데이터 | AWS Athena (httpx) | 대용량 리뷰 원본 조회 |
| 자동화 | n8n 웹훅 | 스케줄링, 자동 동기화 |
| 화면 | Jinja2 + Vanilla JS | HTML 대시보드 렌더링 |

---

## 3. 폴더 구조

> "파일들이 어떻게 정리되어 있나?"

### 전체 구조 (레이어드 아키텍처)

```
Review_Summary_AI/
│
├── app/                          # 핵심 애플리케이션 코드 전체
│   ├── main.py                   # 서버 시작점 (전원 버튼)
│   │
│   ├── api/v1/endpoints/         # API 주소 정의 (15개 파일)
│   │   ├── summaries.py          #   요약 관련 API
│   │   ├── tags.py               #   태그 관련 API
│   │   ├── sentiment.py          #   감정 통계 API
│   │   ├── analysis.py           #   리뷰 분석/필터 API
│   │   ├── report.py             #   리포트 생성/PDF API
│   │   ├── sync.py               #   리뷰 동기화 API
│   │   ├── presets.py            #   프롬프트 프리셋 API
│   │   ├── pipeline_console.py   #   파이프라인 실행/상태 API
│   │   ├── n8n.py                #   n8n 웹훅 API
│   │   ├── n8n_scheduler.py      #   스케줄러 관리 API
│   │   ├── public.py             #   외부 파트너용 API
│   │   ├── pages.py              #   웹 페이지 라우팅
│   │   └── deps.py               #   서비스 생성/주입
│   │
│   ├── services/                 # 비즈니스 로직 (11개 서비스)
│   │   ├── summary_service.py    #   요약 조회/수정/승인
│   │   ├── summary_generator.py  #   AI 요약 생성 (LLM 호출)
│   │   ├── report_service.py     #   리포트 생성 (4단계 파이프라인)
│   │   ├── report_ai_generator.py#   AI 리포트 텍스트 생성
│   │   ├── report_cache_service.py#  리포트 캐시 관리
│   │   ├── sync_service.py       #   Athena ↔ DB 리뷰 동기화
│   │   ├── analysis_service.py   #   리뷰 검색/필터링
│   │   ├── tag_service.py        #   태그 CRUD + 자동 매핑
│   │   ├── tag_stats_calculator.py#  태그 통계 계산
│   │   ├── vehicle_analyzer.py   #   차량 모델별 분석
│   │   └── *_job_service.py      #   비동기 작업 관리 (4개)
│   │
│   ├── domain/                   # 핵심 분석 엔진
│   │   ├── analysis/             #   NLP 분석 모듈
│   │   │   ├── patterns.py       #     감정/태그 패턴 정의 (200+ 규칙)
│   │   │   ├── sentiment_core.py #     감정 판정 핵심 함수
│   │   │   ├── sentiment_utils.py#     통합 감정 분석기
│   │   │   ├── absa.py           #     절(문장) 단위 감정 분석
│   │   │   ├── hybrid_classifier.py#   규칙+임베딩 태그 분류기
│   │   │   ├── tag_embeddings.py #     문장 유사도 비교 엔진
│   │   │   ├── extractor.py      #     키워드 추출 (Kiwi)
│   │   │   ├── chunker.py        #     문장 → 절 분리
│   │   │   └── aggregator.py     #     분석 결과 집계
│   │   │
│   │   └── pipeline/             #   데이터 처리 파이프라인
│   │       ├── unified_pipeline.py#    배치 처리 (9단계)
│   │       ├── realtime_pipeline.py#   실시간 단건 처리
│   │       └── steps/            #     각 단계별 처리기 (8개)
│   │
│   ├── repository/               # 데이터베이스 접근 (11개 파일)
│   │   ├── database.py           #   DB 연결/세션 관리
│   │   ├── orm_models.py         #   테이블 정의 (Python 코드)
│   │   ├── review_repository.py  #   리뷰 데이터 읽기/쓰기
│   │   ├── summary_repository.py #   요약 데이터 읽기/쓰기
│   │   ├── tag_repository.py     #   태그 데이터 읽기/쓰기
│   │   └── ...                   #   기타 테이블별 접근
│   │
│   ├── infrastructure/           # 외부 서비스 연결
│   │   ├── athena_client.py      #   AWS Athena (리뷰 원본 조회)
│   │   ├── llm/                  #   AI (LLM) 관련
│   │   │   ├── openai_provider.py#     OpenAI GPT 호출
│   │   │   ├── prompts.py        #     AI에게 보내는 지시문(프롬프트)
│   │   │   ├── rate_limiter.py   #     API 호출 속도 제한
│   │   │   └── validator.py      #     AI 응답 검증
│   │   └── pdf/                  #   PDF 생성
│   │       ├── generator.py      #     PDF 파일 생성기
│   │       └── cache.py          #     PDF 임시 저장
│   │
│   ├── models/                   # 데이터 형태 정의
│   │   ├── review.py             #   리뷰 데이터 형태
│   │   ├── summary.py            #   요약 데이터 형태
│   │   ├── tag.py                #   태그 데이터 형태
│   │   └── sentiment.py          #   감정 데이터 형태
│   │
│   ├── schemas/                  # API 입출력 형태 정의
│   │   ├── dto.py                #   파이프라인 데이터 형태
│   │   ├── report.py             #   리포트 데이터 형태
│   │   └── ...                   #   기타 API 요청/응답 형태
│   │
│   ├── core/                     # 공통 설정
│   │   ├── config.py             #   환경 설정 (DB 주소, API 키 등)
│   │   ├── constants.py          #   상수값 모음
│   │   ├── middleware.py         #   요청 전/후 처리
│   │   └── stopwords.py          #   불용어 목록 ("은", "는", "이" 등)
│   │
│   ├── templates/                # 화면 HTML 파일
│   │   ├── dashboard_v2.html     #   메인 대시보드
│   │   ├── analysis.html         #   리뷰 분석 페이지
│   │   └── pdf/                  #   PDF 리포트 템플릿
│   │
│   ├── static/                   # 정적 파일 (CSS, JS, 이미지)
│   │   ├── css/shared-theme.css  #   공통 디자인
│   │   └── js/shared-utils.js    #   공통 기능 (알림, API 호출 등)
│   │
│   └── scripts/                  # 운영 스크립트 (수동 실행용)
│       ├── run_full_pipeline.py  #   전체 파이프라인 수동 실행
│       ├── run_batch_summary.py  #   일괄 요약 생성
│       ├── retag_reviews.py      #   리뷰 재분류
│       └── import_from_csv.py    #   CSV 데이터 가져오기
│
├── tests/                        # 테스트 코드
├── ddl/                          # DB 구조 변경 기록 (마이그레이션)
├── .env                          # 환경 변수 (비밀번호, API 키 등)
└── Makefile                      # 자주 쓰는 명령어 모음
```


---

## 4. 데이터베이스 구조

> "데이터가 어떻게 저장되어 있나?"

### 테이블 분류 (6개 그룹, 총 27개)

---

#### 그룹 1: 핵심 데이터 (리뷰 + 지점)

| 테이블 | 데이터 수 | 한줄 설명 |
|--------|----------|-----------|
| **branch_reviews** | 222,024건 | 고객이 작성한 리뷰 원본 전부 (내용, 별점, 감정, 차종 등) |
| **branches** | 1,408건 | 렌트카 지점 ID와 이름 목록 |
| **affiliates** | 390건 | 렌트카 업체 상세 정보 (주소, 전화번호, 위치 등) |

**branch_reviews 주요 항목:**

| 항목 | 설명 | 예시 |
|------|------|------|
| review_id | 리뷰 고유번호 | 12345 |
| branch_name | 지점 이름 | "제주공항점" |
| company_name | 업체 이름 | "롯데렌터카" |
| content | 리뷰 내용 | "직원이 친절하고 차가 깨끗했어요" |
| rating_service | 서비스 별점 (1~5) | 4.5 |
| rating_car | 차량 별점 (1~5) | 4.0 |
| sentiment | AI가 판단한 감정 | "positive" / "negative" / "neutral" |
| car_model | 렌트한 차 종류 | "아반떼" |
| review_date | 리뷰 작성 날짜 | 2026-01-15 |

---

#### 그룹 2: AI 요약

| 테이블 | 데이터 수 | 한줄 설명 |
|--------|----------|-----------|
| **branch_summaries** | 1,478건 | 지점별 AI 요약 결과 (1개월/3개월/6개월/1년/전체) |
| **branch_summaries_history** | 0건 | 요약 변경 이전 버전 백업 (아직 미사용) |
| **branch_summary_log** | 1건 | 요약 생성 기록 (언제, 왜 생성했는지) |
| **branch_scheduler_settings** | 10건 | 자동 요약 생성 주기 설정 |
| **prompt_presets** | - | AI에게 보내는 지시문 템플릿 저장 |

**branch_summaries 주요 항목:**

| 항목 | 설명 | 예시 |
|------|------|------|
| branch_name | 지점 이름 | "제주공항점" |
| review_count | 총 리뷰 수 | 250 |
| avg_rating | 평균 별점 | 4.2 |
| keyword_1~3 | 상위 키워드 3개 | "친절", "청결", "가격" |
| summary_1m | 최근 1개월 AI 요약 | "최근 한 달간 서비스 만족도가..." |
| summary_3m | 최근 3개월 AI 요약 | "3개월간 차량 청결도에 대한..." |
| summary_6m / 1y / all | 6개월 / 1년 / 전체 AI 요약 | (같은 형식) |
| pending_summaries | 승인 대기 중인 새 요약 | {"1m": "새 요약 텍스트..."} |
| status | 상태 | "draft" (초안) / "published" (게시됨) |

---

#### 그룹 3: 태그 시스템 (리뷰 주제 분류)

| 테이블 | 데이터 수 | 한줄 설명 |
|--------|----------|-----------|
| **categories** | 9건 | 태그 대분류 카테고리 (직원친절, 청결, 가격 등) |
| **tags** | 59건 | 세부 태그 52개+ (각 카테고리 안의 상세 주제) |
| **keyword_mappings** | 15,859건 | 키워드 → 태그 연결 ("깨끗" → 차량청결) |
| **branch_keywords** | 236,337건 | 지점별 키워드 등장 횟수 |
| **review_tag_mappings** | 383,531건 | 리뷰별 태그 매핑 (어떤 리뷰에 어떤 태그가 매겨졌는지) |
| **branch_tags** | 35,152건 | 지점별 태그 통계 (태그별 긍정/부정 몇 건인지) |

**9개 카테고리와 역할:**

| 카테고리 | 좋을 때 표현 | 나쁠 때 표현 | 태그 수 |
|----------|-------------|-------------|---------|
| 직원친절 | 직원이 친절함 | 직원이 불친절함 | 8개 |
| 외관 | 차량외관이 좋음 | 차량외관이 별로임 | 9개 |
| 가격 | 가격이 저렴함 | 가격이 비쌈 | 8개 |
| 청결 | 차량이 청결함 | 차량이 더러움 | 7개 |
| 사고 처리 | 사고 처리를 잘해줌 | 사고 처리가 별로임 | 7개 |
| 주유비 | 주유비 부담 없음 | 주유비 부담 높음 | 5개 |
| 배달/배차 | 배달/배차가 우수함 | 배달/배차가 별로임 | 4개 |
| 반납/픽업 | 반납/픽업이 원활함 | 반납/픽업이 불편함 | 1개 |
| 위치/접근성 | 위치/접근성이 좋음 | 위치/접근성이 불편함 | 3개 |

---

#### 그룹 4: 월별 통계 (추이 추적)

| 테이블 | 데이터 수 | 한줄 설명 |
|--------|----------|-----------|
| **monthly_rating_stats** | 6,312건 | 지점별 월별 평균 별점 추이 |
| **monthly_sentiment_stats** | 6,312건 | 지점별 월별 긍정/부정/중립 리뷰 수 추이 |
| **monthly_tag_stats** | 64,347건 | 지점별 월별 태그별 긍정/부정 언급 수 추이 |

---

#### 그룹 5: 차량 분석

| 테이블 | 데이터 수 | 한줄 설명 |
|--------|----------|-----------|
| **car_models_master** | 553건 | 차량 모델 목록 (아반떼, 쏘나타 등) |
| **branch_car_models** | 6,221건 | 어떤 지점에서 어떤 차량이 대여됐는지 |
| **monthly_car_model_tag_stats** | 44,876건 | 차량 모델별 월별 태그 평가 추이 |

---

#### 그룹 6: 리포트 & 시스템 관리

| 테이블 | 데이터 수 | 한줄 설명 |
|--------|----------|-----------|
| **branch_reports** | 152건 | 지점별 종합 분석 리포트 (JSON) |
| **report_jobs** | 170건 | 리포트 생성 작업 진행 상태 (0%~100%) |
| **sync_metadata** | 3건 | 리뷰 동기화 상태 (마지막 동기화 시간, 실행 중 여부) |
| **audit_logs** | 7,944건 | 모든 데이터 변경 기록 (누가, 언제, 뭘 바꿨는지) |
| **schedule_groups** | - | 스케줄러 그룹 설정 |
| **scheduler_targets** | - | 스케줄러 대상 지점 |

---

### 테이블 관계도 (데이터가 어떻게 연결되는지)

```
[업체 정보]          [지점 정보]           [리뷰 원본 22만건]
affiliates ─────── branches ─────────── branch_reviews
                      │                        │
        ┌─────────────┼────────────────────────┤
        │             │                        │
        v             v                        v
  [AI 요약]     [태그 통계]              [리뷰-태그 매핑]
  branch_        branch_tags              review_tag_
  summaries      (긍정/부정 몇건?)         mappings
        │             │                        │
        v             v                        v
  [리포트]      [월별 추이]               [태그 목록]
  branch_        monthly_tag_stats         tags
  reports                                     │
                                              v
                                        [카테고리]
                                        categories (9개)
```

---

## 5. 핵심 기능 설명

### 기능 1: 리뷰 동기화

> "외부에서 새 리뷰를 가져와서 우리 DB에 저장하는 기능"

**작동 방식:**
1. AWS Athena(대용량 리뷰 창고)에서 새 리뷰를 가져옴
2. 중복된 리뷰 제거
3. 잘못 배정된 리뷰 필터링 (A업체 리뷰가 B지점에 잘못 들어온 경우)
4. 우리 DB에 저장
5. 저장된 새 리뷰에 대해 자동으로 분석 파이프라인 실행

**실행 시점:** 매일 새벽 06:00 자동 실행 (n8n 스케줄러) 또는 운영팀이 수동 실행

---

### 기능 2: 감정 분석

> "리뷰가 긍정인지, 부정인지, 중립인지 자동 판별"

**작동 방식:**
1. 리뷰 텍스트에서 감정 키워드를 찾음 ("친절" → 긍정, "더러" → 부정)
2. 문맥을 고려함 ("불편함이 없다" → 이중부정 → 긍정)
3. "~지만" 같은 양보 구문이 있으면 뒤쪽 감정을 우선 적용
4. 별점 정보도 함께 고려하여 최종 판정

**결과:** 각 리뷰에 `positive(긍정)`, `negative(부정)`, `neutral(중립)` 중 하나가 매겨짐

---

### 기능 3: 태그 분류

> "리뷰가 어떤 주제에 대한 이야기인지 자동 분류 (52개 세부 태그)"

**작동 방식:**
1. **규칙 매칭** (빠름): "친절" → 직원친절, "깨끗" → 청결 등 미리 정해진 규칙으로 즉시 분류
2. **임베딩 매칭** (정확함): 규칙으로 못 찾은 키워드는 AI가 문장 의미를 비교하여 가장 비슷한 태그를 찾음
3. 두 방법을 조합하여 최종 태그 결정 (하이브리드 방식)

**결과:** 각 리뷰에 관련 태그가 매겨짐 (예: "직원이 친절하고 차가 깨끗해요" → [직원친절(긍정), 청결(긍정)])

---

### 기능 4: AI 요약

> "지점별로 쌓인 수백 건의 리뷰를 AI가 핵심만 뽑아서 요약"

**작동 방식:**
1. 해당 지점의 리뷰 수가 30건 이상인 기간을 선택 (1개월 → 3개월 → 6개월 → 1년 → 전체)
2. 태그 통계 + 감정 통계 + 대표 리뷰 샘플을 모음
3. OpenAI GPT-4o-mini에게 데이터를 보내서 요약 요청
4. AI가 생성한 요약에서 마크다운 기호 제거, 숫자 정확성 검증
5. DB에 저장 (5개 기간별: 1개월, 3개월, 6개월, 1년, 전체)

**결과:** "제주공항점은 최근 3개월간 직원 친절도에서 높은 만족도를 보이고 있으나, 차량 청결도 개선이 필요합니다..."

---

### 기능 5: 컨설팅 리포트 + PDF

> "지점에 대한 종합 분석 리포트를 만들고 PDF로 내려받기"

**4단계 생성 과정:**

| 단계 | 진행률 | 하는 일 |
|------|--------|---------|
| 1단계: 데이터 수집 | 10% | 지점 정보, 차량 분석, 리뷰 샘플 모으기 |
| 2단계: 태그 분석 | 35% | 카테고리별 강점/약점 판정, 태그 순위 |
| 2.5단계: 인사이트 도출 | 55% | 이전 대비 추세(상승/하강), 같은 지역 벤치마크, 우선 개선사항 |
| 3단계: AI 작성 | 80% | GPT가 기간별 요약 텍스트 생성 |
| 4단계: 조립 및 저장 | 100% | 리포트 완성, DB 저장, PDF 생성 |

**결과:** 종합 분석 리포트 (웹 조회 + PDF 다운로드 가능)

---

## 6. 워크플로우 (데이터 흐름)

> "데이터가 처음부터 끝까지 어떻게 흘러가는지"

### 전체 흐름 요약

```
[1] 리뷰 수집          [2] 자동 분석           [3] AI 가공           [4] 결과물
──────────────       ──────────────        ──────────────       ──────────────
Carmore 고객이       시스템이 자동으로:      AI(GPT)가:           운영팀이 확인:
리뷰 작성            - 감정 분석            - 요약 생성           - 대시보드
     │               - 태그 분류            - 리포트 작성         - PDF 리포트
     v               - 통계 집계                                 - Excel 내보내기
AWS Athena에              │                      │
원본 저장                  v                      v
     │              DB 통계 테이블          branch_summaries
     v              갱신 완료              branch_reports
우리 DB에                                  저장 완료
동기화
```

### 9단계 배치 파이프라인 (상세)

리뷰가 동기화된 후, 다음 9단계를 순서대로 처리합니다:

| 단계 | 이름 | 하는 일 | 결과물 |
|------|------|---------|--------|
| **1** | 전처리 | 욕설/광고 필터링, 키워드 추출, 감정 분석, 태그 분류 | 분석된 리뷰 데이터 |
| **2** | 감정 저장 | 각 리뷰의 감정(긍정/부정/중립) DB에 저장 | branch_reviews.sentiment 갱신 |
| **4** | 태그 집계 | 새 키워드 자동 매핑, 지점별 태그 통계 갱신 | branch_tags 갱신 |
| **5** | 차량 집계 | 차량 모델 정보 갱신 | car_models_master 갱신 |
| **6** | 키워드 관리 | 지점별 키워드 빈도수 갱신 | branch_keywords 갱신 |
| **7** | 태그 매핑 저장 | 리뷰-태그 개별 매핑 기록 저장 | review_tag_mappings 저장 |
| **8** | 월별 통계 | 월별 별점/감정/태그 통계 갱신 | monthly_*_stats 갱신 |
| **9** | 차량 월별 통계 | 차량 모델별 월별 태그 추이 저장 | monthly_car_model_tag_stats 저장 |

### 실제 예시: 리뷰 1건의 처리 과정

```
[입력] "직원이 친절했지만 차량이 더러웠어요. 가격은 합리적이었습니다."
       별점: 서비스 4.5, 차량 2.0, 편의성 4.0

  1단계: 문장을 절로 분리
  → ["직원이 친절했지만", "차량이 더러웠어요", "가격은 합리적이었습니다"]

  2단계: 키워드 추출 (Kiwi 한국어 분석)
  → ["직원", "친절", "차량", "더러", "가격", "합리"]

  3단계: 각 절의 감정 분석
  → "직원이 친절했지만" → 직원친절 카테고리, 긍정
  → "차량이 더러웠어요" → 청결 카테고리, 부정
  → "가격은 합리적이었습니다" → 가격 카테고리, 긍정

  4단계: 태그 분류
  → "친절" → 규칙 매칭 → 직원친절 태그 (즉시)
  → "더러" → 규칙 매칭 → 청결 태그 (즉시)
  → "합리" → 규칙 매칭 → 가격 태그 (즉시)

  5단계: 양보 구문 처리
  → "~지만" 감지 → 앞절(긍정)과 뒷절(부정) 모두 유지

  6단계: 최종 감정 판정
  → 텍스트: 긍정 2건 vs 부정 1건 (혼합)
  → 별점: 최저 2.0 (부정 신호) + 평균 3.5 (중립)
  → 종합 → "neutral" (중립)

[결과]
  리뷰 감정: neutral
  태그: 직원친절(긍정), 청결(부정), 가격(긍정)
  → DB 통계 테이블에 각각 +1 반영
```

---

## 7. 분석 알고리즘

> "리뷰를 어떻게 분석하는지 상세 원리"

### 7-1. 감정 분석 알고리즘

**방식:** 규칙 기반 + 문맥 분석 하이브리드

```
[리뷰 텍스트]
     │
     v
[1] 키워드 앞뒤 50글자를 확인 (문맥 윈도우)
     │
     v
[2] 긍정/부정 키워드 패턴 매칭 (200개 이상의 규칙)
    - 부정 키워드: "불친절", "비싸", "냄새", "아쉬운" 등
    - 긍정 키워드: "친절", "깨끗", "합리적", "빠른" 등
     │
     v
[3] 특수 구문 처리
    - 이중부정: "불편함이 없다" → 부정+부정 = 긍정
    - 양보구문: "친절했지만 더러웠다" → 뒷절(더러웠다) 우선
    - 같은 카테고리에 긍정+부정 공존 → 다수결 또는 뒷절 우선
     │
     v
[4] 별점 정보 종합
    - 최저 별점이 3점 미만이면 부정 신호
    - 텍스트 분석 결과와 별점 결과를 종합하여 최종 판정
     │
     v
[결과] positive / negative / neutral + 신뢰도(0~1)
```

### 7-2. 태그 분류 알고리즘

**방식:** 2단계 하이브리드 (규칙 → 임베딩)

```
[키워드] 예: "깨끗"
     │
     v
[1단계: 규칙 매칭] (초고속, 즉시 분류)
    TAG_REGISTRY에 미리 정의된 규칙 검색
    "깨끗" → 청결 카테고리에 있음 → 즉시 분류 완료!
     │
     ├─ 매칭 성공 → 끝
     │
     └─ 매칭 실패 → 2단계로
              │
              v
[2단계: 임베딩 매칭] (정확, AI 의미 비교)
    FastEmbed가 키워드의 "의미"를 숫자로 변환
    52개 태그의 "의미"와 유사도 비교
    유사도 50% 이상 → 해당 태그로 분류
    유사도 50% 미만 → "일반" 태그 (분류 불가)
     │
     v
[결과] {태그명, 감정(긍정/부정), 신뢰도, 매칭 방법(rule/embedding)}
```

**핵심 수치:**
- 규칙 매칭: 즉시 분류 (O(1) - 규칙 수에 관계없이 일정 속도)
- 임베딩 유사도 기준: 50% (0.50) 이상이면 해당 태그로 분류
- 임베딩 모델: `paraphrase-multilingual-MiniLM-L12-v2` (다국어 문장 유사도 모델)

### 7-3. AI 요약 알고리즘

```
[입력 데이터 준비]
     │
     v
[1] 기간 선택: 리뷰 30건 이상인 가장 짧은 기간 선택
    1개월(30건+) → 3개월 → 6개월 → 1년 → 전체
     │
     v
[2] 데이터 수집:
    - 태그 통계 (긍정/부정 태그 순위)
    - 감정 통계 (긍정/부정/중립 비율)
    - 대표 리뷰 샘플 (감정별)
     │
     v
[3] AI 호출: GPT-4o-mini에게 프롬프트 + 데이터 전달
    - 운영 모드: 개선점 중심
    - 마케팅 모드: 강점 중심
     │
     v
[4] 후처리:
    - 마크다운 기호 제거 (#, *, ** 등)
    - 숫자 검증 (AI가 만들어낸 틀린 숫자 교정)
     │
     v
[결과] 5개 기간별 요약 텍스트 → DB 저장
```

---

## 8. 주요 API 주소 목록

> "시스템에 어떤 요청을 보낼 수 있는지" — 핵심 3가지만 정리

API는 **시스템에 요청을 보내는 주소**입니다. 웹 브라우저에서 주소를 입력하면 페이지가 열리듯이, 프로그램이 이 주소로 요청을 보내면 데이터를 받거나 작업을 시작할 수 있습니다.

### AI 요약 API (`/api/summaries`)

> 지점별 AI 리뷰 요약을 조회하고 생성하는 핵심 API

| 동작 | 주소 | 설명 |
|------|------|------|
| 조회 | `GET /summaries` | 요약 목록 가져오기 |
| 조회 | `GET /summaries/{지점ID}` | 특정 지점 요약 상세 |
| 실행 | `POST /summaries/{지점ID}/regenerate` | AI 요약 재생성 |
| 실행 | `POST /summaries/{지점ID}/apply-pending` | 대기 중인 요약 승인 |
| 실행 | `POST /summaries/{지점ID}/discard-pending` | 대기 중인 요약 거부 |

### AI 리포트 API (`/api/reports`)

> 지점별 종합 컨설팅 리포트를 생성하고 PDF로 다운로드하는 핵심 API

| 동작 | 주소 | 설명 |
|------|------|------|
| 실행 | `POST /reports/{지점ID}/generate/async` | 리포트 생성 시작 (작업ID 반환) |
| 조회 | `GET /reports/{지점ID}/job/{작업ID}` | 생성 진행률 확인 (0~100%) |
| 조회 | `GET /reports/{지점ID}` | 리포트 조회 (없으면 새로 생성) |
| 다운로드 | `GET /reports/{지점ID}/pdf` | PDF 다운로드 |
| 실행 | `POST /reports/batch-pdf` | 여러 지점 PDF 일괄 생성 |
| 다운로드 | `GET /reports/batch-pdf/download/{작업ID}` | 일괄 PDF 다운로드 |
| 삭제 | `DELETE /reports/{지점ID}` | 리포트 삭제 |

### 태그 API (`/api/tags`)

> 리뷰 주제 분류(52개 태그)를 관리하는 API

| 동작 | 주소 | 설명 |
|------|------|------|
| 조회 | `GET /tags/categories` | 카테고리 목록 (9개) |
| 조회 | `GET /tags/list` | 태그 목록 (페이지별) |
| 조회 | `POST /tags/batch` | 지점별 태그 일괄 조회 |
| 생성 | `POST /tags/` | 새 태그 추가 |
| 수정 | `PUT /tags/{태그ID}` | 태그 정보 수정 |
| 삭제 | `DELETE /tags/{태그ID}` | 태그 삭제 |

---

## 9. 화면(대시보드) 구성

### 메인 대시보드 (`/`)

**역할:** 전체 지점의 요약 상태를 한눈에 보는 메인 화면

**주요 기능:**
- 지점별 요약 카드 목록 (지점명, 평균별점, 리뷰수, AI 요약)
- 지역 필터 (서울, 부산, 제주 등 17개 광역시/도)
- 즐겨찾기 (자주 보는 지점을 상단에 고정)
- 요약 재생성 버튼 (AI가 새로 요약 생성)
- 리포트 생성 버튼 (PDF 다운로드 가능한 리포트 생성)
- 페이지네이션 (한 페이지당 30개 지점)

### 리뷰 분석 페이지 (`/analysis`)

**역할:** 개별 리뷰를 직접 검색하고 필터링하는 상세 분석 화면

**주요 기능:**
- 리뷰 원본 텍스트 목록
- 다중 필터: 지역, 업체, 지점, 감정(긍정/부정/중립), 태그, 기간
- Excel 내보내기 (필터링 결과를 엑셀 파일로 다운로드)

### 파이프라인 콘솔 (`/pipeline-console`)

**역할:** 배치 분석 작업의 진행 상황을 실시간 모니터링

**주요 기능:**
- 현재 진행 중인 작업 목록
- 단계별 진행률 표시 (0~100%)
- 에러 발생 시 에러 메시지 표시
- 작업 취소 기능

### 스케줄러 페이지 (`/scheduler`)

**역할:** 자동 실행 스케줄 관리

**주요 기능:**
- 워크플로우별 스케줄 목록
- 크론 표현식 설정 (예: "매일 새벽 6시" = `0 6 * * *`)
- 스케줄 활성/비활성 토글
- 즉시 실행 버튼

---

## 10. 주요 파일별 함수 설명

> "각 파일이 어떤 일을 하는 함수들을 갖고 있는지"

### 서비스 계층 (app/services/)

#### summary_service.py — 요약 관리

| 함수 | 하는 일 |
|------|---------|
| `get_summaries(filters)` | 필터 조건에 맞는 요약 목록을 가져옴 |
| `get_summary_detail(branch_id)` | 특정 지점의 상세 요약을 가져옴 |
| `update_summary(branch_id, data)` | 요약 내용을 수정함 |
| `generate_pending_summary(branch_id)` | 새 AI 요약을 생성하되, 승인 대기 상태로 저장 |
| `approve_pending_summary(branch_id)` | 대기 중인 요약을 승인하여 게시 |

#### summary_generator.py — AI 요약 생성

| 함수 | 하는 일 |
|------|---------|
| `generate_summary_with_data(branch_id)` | 특정 지점의 AI 요약을 처음부터 끝까지 생성 |
| `_select_period_and_fetch_reviews()` | 리뷰 30건 이상인 최적 기간을 선택 |
| `_fetch_tag_and_sentiment_data()` | 태그 통계 + 감정 통계를 DB에서 조회 |
| `_fetch_representative_reviews()` | 감정별 대표 리뷰를 샘플링 |
| `_call_llm()` | OpenAI GPT에게 요약 요청을 보냄 |
| `_postprocess_summary()` | AI 응답에서 마크다운 제거, 숫자 검증 |

#### report_service.py — 리포트 생성 (4단계)

| 함수 | 하는 일 |
|------|---------|
| `generate_report_with_progress(branch_id)` | 4단계 리포트 전체 생성 (진행률 콜백 포함) |
| `_step_collect()` | 1단계: 지점 정보 + 차량 분석 + 리뷰 샘플 수집 |
| `_step_tags()` | 2단계: 태그 통계 계산, 강점/약점 판정 |
| `_step_insights()` | 2.5단계: 트렌드 분석, 벤치마크, 우선 개선사항 |
| `_step_ai()` | 3단계: AI가 기간별 요약 텍스트 생성 |
| `_step_build()` | 4단계: 리포트 조립, DB 저장, PDF 생성 |
| `get_report(branch_id)` | 기존 리포트 조회 (없으면 새로 생성) |
| `delete_report(branch_id)` | 리포트 삭제 |

#### sync_service.py — 리뷰 동기화

| 함수 | 하는 일 |
|------|---------|
| `sync_reviews(date_from, date_to)` | 지정 기간의 리뷰를 Athena에서 가져와 DB에 저장 |
| `_process_day_chunk()` | 하루치 리뷰를 처리 (조회 → 필터링 → 저장 → 파이프라인) |
| `_filter_misrouted_reviews()` | 잘못 배정된 리뷰 필터링 (80% 다수결 기준) |

#### tag_service.py — 태그 관리

| 함수 | 하는 일 |
|------|---------|
| `get_categories()` | 태그 카테고리 목록 (9개) 조회 |
| `get_tags(page, size)` | 태그 목록 (페이지별) 조회 |
| `create_tag(data)` | 새 태그 생성 |
| `update_tag(id, data)` | 태그 정보 수정 |
| `delete_tag(id)` | 태그 삭제 |
| `get_branch_tags_batch()` | 여러 지점의 태그를 한 번에 조회 |

#### analysis_service.py — 리뷰 검색/분석

| 함수 | 하는 일 |
|------|---------|
| `get_filter_options()` | 필터에 사용할 지역/업체/지점 목록 조회 |
| `get_reviews(filters)` | 조건에 맞는 리뷰 목록 조회 |
| `export_reviews(filters)` | 필터링된 리뷰를 Excel 파일로 내보내기 |

#### vehicle_analyzer.py — 차량 분석

| 함수 | 하는 일 |
|------|---------|
| `analyze(branch_id)` | 특정 지점의 차량 모델별 평가 분석 |

#### tag_stats_calculator.py — 태그 통계 계산

| 함수 | 하는 일 |
|------|---------|
| `compute(branch_id, period)` | 카테고리별 집계, 강점/약점 판정, 태그 순위 계산 |

---

### 도메인 계층 (app/domain/)

#### analysis/patterns.py — 감정/태그 규칙 정의

| 데이터 | 내용 |
|--------|------|
| `TAG_REGISTRY` | 52개 태그의 키워드-태그 매핑 규칙 |
| `RULE_BASED_TAG_MAPPING` | 규칙 기반 빠른 분류용 매핑 |
| `NEGATIVE_PATTERNS` | 부정 감정 키워드 120개+ ("불친절", "비싸", "냄새" 등) |
| `POSITIVE_PATTERNS` | 긍정 감정 키워드 ("친절", "깨끗", "합리적" 등) |
| `CONCESSION_REGEX` | 양보 구문 패턴 ("~지만", "~는데" 등) |

#### analysis/sentiment_core.py — 감정 판정 핵심

| 함수 | 하는 일 |
|------|---------|
| `determine_sentiment()` | 키워드 + 문맥 + 별점을 종합하여 최종 감정 판정 |
| `_check_double_negation()` | 이중부정 처리 ("불편함이 없다" → 긍정) |
| `_resolve_conflict()` | 같은 카테고리 내 긍정/부정 충돌 시 해소 |

#### analysis/hybrid_classifier.py — 하이브리드 태그 분류기

| 함수 | 하는 일 |
|------|---------|
| `classify_review(text, keywords)` | 리뷰 텍스트와 키워드를 받아 태그 분류 결과 반환 |
| `_rule_classify()` | 1단계: 규칙 기반 빠른 분류 (즉시) |
| `_embedding_classify()` | 2단계: 임베딩 유사도 기반 정밀 분류 |

#### analysis/absa.py — 절 단위 감정 분석

| 함수 | 하는 일 |
|------|---------|
| `analyze(text)` | 문장을 절로 쪼갠 뒤, 각 절의 주제(Aspect)와 감정(Opinion) 분석 |

#### analysis/extractor.py — 키워드 추출

| 함수 | 하는 일 |
|------|---------|
| `extract_keywords(text)` | Kiwi로 문장을 형태소 분석하여 의미있는 키워드만 추출 |

#### analysis/tag_embeddings.py — 문장 유사도 비교

| 함수 | 하는 일 |
|------|---------|
| `find_similar_tags(keyword)` | 키워드와 가장 유사한 태그를 임베딩 비교로 찾음 (유사도 0.50 이상) |

#### analysis/chunker.py — 문장 분리

| 함수 | 하는 일 |
|------|---------|
| `chunk(text)` | 접속사("~지만", "~는데", "~고")를 기준으로 문장을 절 단위로 분리 |

---

### 파이프라인 계층 (app/domain/pipeline/)

#### unified_pipeline.py — 9단계 배치 처리

| 함수 | 하는 일 |
|------|---------|
| `run(reviews)` | 리뷰 목록을 받아 9단계 처리를 순서대로 실행 |
| `_step_preprocess()` | 1단계: 전처리 + 키워드 + 감정 + 태그 (CPU 집약적) |
| `_step_update_review_sentiment()` | 2단계: 감정 결과 DB에 저장 |
| 나머지 step들 | 4~9단계: 각 집계 테이블 갱신 |

#### steps/ — 각 단계별 처리기

| 파일 | 하는 일 |
|------|---------|
| `preprocessor.py` | 리뷰 전처리 + NLP 분석 (키워드, 감정, 태그) |
| `review_updater.py` | 리뷰 감정 결과 DB 업데이트 |
| `tag_aggregator.py` | 태그 통계 집계 (branch_tags) |
| `car_model_tags.py` | 차량 모델 태그 집계 |
| `keyword_manager.py` | 키워드 빈도 관리 (branch_keywords) |
| `review_tag_mapper.py` | 리뷰-태그 매핑 저장 (review_tag_mappings) |
| `monthly_stats.py` | 월별 통계 갱신 (별점, 감정, 태그) |
| `monthly_car_model_stats.py` | 차량별 월별 태그 통계 갱신 |

---

### 인프라 계층 (app/infrastructure/)

#### athena_client.py — AWS Athena 연결

| 함수 | 하는 일 |
|------|---------|
| `fetch_reviews(since, until)` | 지정 기간의 리뷰 원본을 Athena에서 조회 |

#### llm/openai_provider.py — AI 호출

| 함수 | 하는 일 |
|------|---------|
| `generate(prompt)` | OpenAI GPT-4o-mini에게 텍스트 생성 요청 |

#### llm/prompts.py — AI 지시문

| 데이터 | 내용 |
|--------|------|
| 요약 프롬프트 | AI에게 "이 리뷰들을 요약해줘"라고 보내는 지시문 |
| 리포트 프롬프트 | AI에게 "이 데이터로 리포트를 써줘"라고 보내는 지시문 |

#### llm/rate_limiter.py — API 속도 제한

| 함수 | 하는 일 |
|------|---------|
| `acquire()` | OpenAI API 호출 속도를 제한하여 과금 폭주 방지 |

#### pdf/generator.py — PDF 생성

| 함수 | 하는 일 |
|------|---------|
| `generate_pdf(report_data)` | 리포트 데이터를 받아 PDF 파일 생성 |

---

### API 계층 (app/api/v1/endpoints/)

각 파일은 해당 기능의 **웹 주소(URL)**를 정의합니다. 위의 [8. API 주소 목록](#8-api-주소-목록) 참조.

#### deps.py — 서비스 생성/연결

| 함수 | 하는 일 |
|------|---------|
| `get_summary_service()` | 요약 서비스 인스턴스 생성 |
| `get_report_service()` | 리포트 서비스 인스턴스 생성 |
| `get_sync_service()` | 동기화 서비스 인스턴스 생성 |
| ... | (각 서비스별 생성 함수) |

---

### DB 접근 계층 (app/repository/)

#### database.py — DB 연결 관리

| 함수 | 하는 일 |
|------|---------|
| `get_session_factory()` | DB 연결 세션을 만들어주는 팩토리 |
| `with_retry()` | DB 연결 실패 시 자동 재시도 |

#### 각 repository 파일 — 테이블별 CRUD

| 파일 | 담당 테이블 | 하는 일 |
|------|------------|---------|
| `review_repository.py` | branch_reviews | 리뷰 저장/조회/수정 |
| `summary_repository.py` | branch_summaries | 요약 저장/조회/수정 |
| `tag_repository.py` | tags, categories | 태그/카테고리 저장/조회 |
| `branch_tag_repository.py` | branch_tags | 지점별 태그 통계 조회 |
| `report_repository.py` | branch_reports | 리포트 저장/조회 |
| `report_job_repository.py` | report_jobs | 작업 상태 관리 |
| `car_model_repository.py` | car_models_master | 차량 모델 관리 |
| `sync_metadata_repository.py` | sync_metadata | 동기화 상태 관리 |
| `affiliate_repository.py` | affiliates | 업체 정보 관리 |

---

### DB 자동 함수 (RPC, 12개)

DB 내부에서 자동으로 실행되는 함수들:

| 함수 | 하는 일 |
|------|---------|
| `upsert_reviews` | 리뷰를 대량으로 추가하거나 기존 건 갱신 |
| `getTagStatsByPeriod` | 기간별 태그 통계를 한 번에 조회 |
| `audit_trigger_func` | 데이터가 변경될 때마다 자동으로 변경 기록 남김 |
| `apply_tag_decay` | 오래된 태그의 가중치를 시간에 따라 감소 |
| `apply_time_decay` | 시간 감쇠 계산 (오래된 데이터일수록 영향력 감소) |
| `cleanup_old_audit_logs` | 오래된 변경 기록 자동 정리 |
| `get_table_stats` | 테이블별 데이터 수 통계 |
| `get_unmapped_keywords` | 아직 태그에 연결되지 않은 키워드 조회 |
| `update_branch_summaries_timestamp` | 요약 수정 시 자동으로 시간 기록 |
| `update_branch_summary_stats` | 요약 통계 자동 갱신 |
| `update_report_jobs_updated_at` | 작업 상태 변경 시 자동 시간 기록 |
| `update_updated_at_column` | 범용 자동 시간 기록 |