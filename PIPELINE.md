# Review Summary AI - 파이프라인 상세 문서

## 1. 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────────┐
│                         데이터 흐름                                  │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   Excel      │     │  Pipeline    │     │  Supabase    │
│  리뷰 데이터  │ ──▶ │   v3.2       │ ──▶ │  PostgreSQL  │
│  (21만건)    │     │              │     │              │
└──────────────┘     └──────────────┘     └──────┬───────┘
                                                  │
                            ┌─────────────────────┘
                            ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   운영팀      │ ◀── │   Flask      │ ◀── │  APScheduler │
│  대시보드     │     │   서버       │     │  스케줄러     │
└──────────────┘     └──────────────┘     └──────────────┘
```

---

## 2. 파이프라인 단계별 프로세스

### Step 1: 데이터 로드
```python
# 입력: Excel 파일
df = pd.read_excel('data/리뷰리스트_20260109.xlsx')
# 출력: 217,660개 리뷰
```

### Step 2: 빈 리뷰 제거
| 필터 조건 | 설명 |
|-----------|------|
| `null` | 빈 값 제거 |
| 빈 문자열 | 공백만 있는 리뷰 제거 |
| 5자 미만 | 의미 없는 짧은 리뷰 제거 |

**결과:** 217,660 → 184,070개 (33,590개 제거)

### Step 3: 블라인드/삭제 제거 + 지점 필터링
```
┌─────────────────┐
│ 블라인드/삭제    │ ──▶ 3,226개 제거
│ 상태 리뷰 제거   │
└─────────────────┘
         │
         ▼
┌─────────────────┐
│ 30개 이하 리뷰   │ ──▶ 소규모 지점 제외
│ 지점 제거        │
└─────────────────┘
```

**결과:** 371개 지점, 175,682개 리뷰

### Step 4: 2단계 하이브리드 감정 분석

```
┌────────────────────────────────────────────────────────────┐
│                    감정 분석 플로우                         │
└────────────────────────────────────────────────────────────┘

                    ┌──────────────┐
                    │  리뷰 텍스트  │
                    └──────┬───────┘
                           │
                           ▼
              ┌────────────────────────┐
              │   1차: Lexicon 분석     │
              │   (사전 기반, 빠름)      │
              └────────────┬───────────┘
                           │
           ┌───────────────┼───────────────┐
           │               │               │
           ▼               ▼               ▼
    ┌──────────┐    ┌──────────┐    ┌──────────┐
    │ score≥0.7│    │0.3<score │    │score≤0.3 │
    │  긍정    │    │  <0.7    │    │  부정    │
    └────┬─────┘    └────┬─────┘    └────┬─────┘
         │               │               │
         │               ▼               │
         │    ┌──────────────────┐       │
         │    │  2차: BERT 분석   │       │
         │    │  (딥러닝, 정밀)   │       │
         │    └────────┬─────────┘       │
         │               │               │
         └───────────────┼───────────────┘
                         ▼
                  ┌──────────────┐
                  │   최종 결과   │
                  │ positive/    │
                  │ neutral/     │
                  │ negative     │
                  └──────────────┘
```

**감정 어휘 사전:**
```python
POSITIVE_WORDS = {
    '좋다', '좋아', '좋은', '최고', '만족', '편리', '친절', '깨끗',
    '빠르다', '신속', '감사', '추천', '훌륭', '완벽', '쾌적', '저렴'
}

NEGATIVE_WORDS = {
    '불친절', '불편', '더럽다', '지저분', '느리다', '비싸다',
    '불만', '실망', '아쉽다', '최악', '짜증', '후회', '환불'
}
```

**결과:**
| 감정 | 건수 | 비율 |
|------|------|------|
| 긍정 | 130,516 | 74.3% |
| 중립 | 40,003 | 22.8% |
| 부정 | 5,163 | 2.9% |

### Step 5: 긍정 리뷰 필터링
```python
df_positive = df[
    (df['sentiment'] == 'positive') &
    (df['sentiment_score'] >= 0.45)
]
```
**결과:** 130,516개 긍정 리뷰 선별

### Step 6: 키워드 추출 (MeCab)

```
┌─────────────────────────────────────────────────────────┐
│                   MeCab 형태소 분석                      │
└─────────────────────────────────────────────────────────┘

입력: "직원분들이 정말 친절하고 차량도 깨끗했어요"
                    │
                    ▼
        ┌───────────────────────┐
        │   MeCab 토크나이징     │
        └───────────────────────┘
                    │
    ┌───────────────┼───────────────┐
    │               │               │
    ▼               ▼               ▼
┌────────┐    ┌────────┐    ┌────────┐
│ 명사   │    │ 형용사  │    │ 기타   │
│ NNG    │    │ VA     │    │ (제외) │
│ NNP    │    │        │    │        │
└────────┘    └────────┘    └────────┘
    │               │
    └───────┬───────┘
            ▼
    ┌───────────────┐
    │  불용어 제거   │
    └───────────────┘
            │
            ▼
출력: ['직원', '친절', '차량', '깨끗']
```

**불용어 목록 (70개):**
```python
STOP_WORDS = {
    '렌트카', '호텔', '예약', '이용', '추천', '정도', '생각',
    '느낌', '그냥', '좀', '약간', '진짜', '정말', '너무', '완전',
    '이번', '다음', '처음', '다시', '계속', '항상', '때문', '위해',
    '것', '수', '등', '중', '내', '더', '안', '잘', '곳', '거',
    '분', '점', '때', '후', '전', '번', '개', '회', '데', '말',
    '사용', '경우', '사람', '시간', '정보', '방법', '상황',
    '여행', '출장', '경험', '기대', '방문', '오늘', '어제',
    '네', '예', '응', '음', '아', '오', '저', '제', '이', '그'
}
```

### Step 7: 지점별 키워드 집계 (가중치 적용)

```
┌─────────────────────────────────────────────────────────┐
│                   가중치 계산 시스템                      │
└─────────────────────────────────────────────────────────┘

리뷰별 가중치 = f(최신성, 공감수, 길이, 별점, 품질)

┌──────────┬────────────────────────────────┐
│  요소    │           가중치               │
├──────────┼────────────────────────────────┤
│ 최신성   │ 1개월 이내: 2.0x               │
│          │ 3개월 이내: 1.5x               │
│          │ 1년 이상: 0.3x                 │
├──────────┼────────────────────────────────┤
│ 공감수   │ 10+: 2.5x, 5+: 1.8x, 1+: 1.3x  │
├──────────┼────────────────────────────────┤
│ 길이     │ 100자+: 1.5x, 50자+: 1.2x      │
│          │ 20자-: 0.3x                    │
├──────────┼────────────────────────────────┤
│ 별점     │ 4.5+: 1.2x, 3.0-: 0.2x         │
└──────────┴────────────────────────────────┘

키워드 점수 = Σ(키워드 출현 × 리뷰 가중치)
```

**출력:** 지점별 TOP 10 키워드

### Step 8: AI 요약 생성

```
┌─────────────────────────────────────────────────────────┐
│                   LLM 요약 생성                          │
└─────────────────────────────────────────────────────────┘

┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│ TOP 10 키워드 │     │ 대표 리뷰 5개 │     │   프롬프트   │
└──────┬───────┘     └──────┬───────┘     └──────┬───────┘
       │                    │                    │
       └────────────────────┼────────────────────┘
                            │
                            ▼
                 ┌──────────────────┐
                 │  GPT-3.5-turbo   │
                 │  (3500 RPM)      │
                 └────────┬─────────┘
                          │
                          ▼
                 ┌──────────────────┐
                 │   2~3줄 요약문    │
                 │   (마케팅용)      │
                 └──────────────────┘
```

**프롬프트:**
```
System: 당신은 20년차 베테랑 프리미엄 렌터카 카모아 마케터입니다.
        렌터카 이용 리뷰를 분석하여 서비스의 장점을 극대화한
        신뢰감 있는 요약 문구를 작성합니다.

가이드라인:
1. 핵심 키워드를 자연스럽게 녹여내세요.
2. 전문적인 어조를 사용하세요.
3. 분량은 2~3줄로 명확하게 작성하세요.
4. 마크다운, 이모지, 특수문자 없이 순수 텍스트만 작성하세요.
```

### Step 9: 결과 저장

```
┌─────────────────────────────────────────────────────────┐
│                     출력 파일                            │
└─────────────────────────────────────────────────────────┘

output/
├── branch_keywords.xlsx      # 지점별 TOP 10 키워드
├── branch_top3_keywords.xlsx # 지점별 TOP 3 키워드
└── branch_summaries.xlsx     # AI 요약 결과

┌─────────────────────────────────────────────────────────┐
│                   Supabase 저장                          │
└─────────────────────────────────────────────────────────┘

summaries 테이블:
┌────────────┬─────────────┬──────────┬────────┐
│ branch_id  │ ai_summary  │ keywords │ status │
├────────────┼─────────────┼──────────┼────────┤
│ 101        │ "친절한..." │ [친절..] │ draft  │
│ 102        │ "깨끗한..." │ [깨끗..] │ draft  │
│ ...        │ ...         │ ...      │ ...    │
└────────────┴─────────────┴──────────┴────────┘
```

---

## 3. 데이터베이스 스키마

```sql
-- 지점 테이블
CREATE TABLE branches (
    id SERIAL PRIMARY KEY,
    branch_id INTEGER UNIQUE NOT NULL,
    name VARCHAR(100)
);

-- 요약 결과 테이블
CREATE TABLE summaries (
    id SERIAL PRIMARY KEY,
    branch_id INTEGER UNIQUE REFERENCES branches(branch_id),
    ai_summary TEXT,
    edited_summary TEXT,
    keywords JSONB DEFAULT '[]',
    review_count INTEGER DEFAULT 0,
    status VARCHAR(20) DEFAULT 'draft'  -- draft/approved/published
);

-- 키워드 테이블 (가중치 포함)
CREATE TABLE branch_keywords (
    branch_id INTEGER,
    keyword VARCHAR(50),
    raw_count INTEGER,
    weighted_score DECIMAL(10,2)
);

-- 스케줄러 설정
CREATE TABLE scheduler_config (
    id SERIAL PRIMARY KEY,
    config_key VARCHAR(50) UNIQUE,
    cron_expression VARCHAR(50),
    is_enabled BOOLEAN DEFAULT true,
    description TEXT,
    months VARCHAR(50)
);

-- 스케줄러 로그
CREATE TABLE scheduler_logs (
    id SERIAL PRIMARY KEY,
    config_key VARCHAR(50),
    started_at TIMESTAMP DEFAULT NOW(),
    finished_at TIMESTAMP,
    branch_count INTEGER,
    success_count INTEGER,
    status VARCHAR(20)
);
```

---

## 4. Flask 운영 대시보드

### API 엔드포인트

```
GET    /api/summaries              전체 목록 조회
GET    /api/summaries/<id>         상세 조회
PUT    /api/summaries/<id>         수정 (edited_summary)
POST   /api/summaries/<id>/approve 승인 (draft → approved)
POST   /api/summaries/<id>/publish 게시 (approved → published)
GET    /api/stats                  통계 조회

GET    /api/scheduler/status       스케줄러 상태
GET    /api/scheduler/config       스케줄 설정 조회
PUT    /api/scheduler/config/<key> 스케줄 설정 변경
POST   /api/scheduler/trigger      수동 실행
GET    /api/scheduler/logs         실행 로그
```

### 스케줄러 설정

| 시즌 | config_key | cron | 월 |
|------|------------|------|-----|
| 성수기 | peak_season | `0 3 * * *` | 7, 8, 12 |
| 일반기 | normal_season | `0 3 * * 1,3,5` | 3~6, 9~11 |
| 비수기 | off_season | `0 3 * * 1` | 1, 2 |

---

## 5. 환경 설정

### .env 파일
```env
# OpenAI
OPENAI_API_KEY=sk-...

# LLM Provider (openai / gemini)
LLM_PROVIDER=openai

# Supabase
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_KEY=eyJ...  # service_role 키 권장
```

---

## 6. 실행 방법

```bash
# 1. 가상환경 활성화
source venv/bin/activate

# 2. 파이프라인 실행 (전체)
python scripts/pipeline_v3.py

# 3. Flask 서버 실행
python web/app.py

# 접속: http://localhost:5000
```

---

## 7. 처리 결과 요약

| 단계 | 입력 | 출력 | 비고 |
|------|------|------|------|
| 로드 | Excel | 217,660 | - |
| 빈 리뷰 제거 | 217,660 | 184,070 | -33,590 |
| 블라인드/삭제 | 184,070 | 175,682 | -8,388 |
| 감정 분석 | 175,682 | 175,682 | 분류만 |
| 긍정 필터 | 175,682 | 130,516 | 74.3% |
| 키워드 추출 | 130,516 | 371 지점 | TOP 10 |
| AI 요약 | 371 | 371 | GPT-3.5 |
| DB 저장 | 371 | 371 | Supabase |

**총 소요 시간:** ~140초
